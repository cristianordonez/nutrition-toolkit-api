version: 2
build:
  os: ubuntu-lts-latest
  tools:
    python: "3.14"
  commands:
    - pip install uv
    - uv venv -p 3.14
    - uv pip install tox tox-uv sphinx sphinx-rtd-theme
    - mkdir -p $READTHEDOCS_OUTPUT/html/
    - sphinx-build -b html docs/ $READTHEDOCS_OUTPUT/html/